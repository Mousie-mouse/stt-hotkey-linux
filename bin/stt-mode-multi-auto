#!/bin/bash

TMP_DIR="/tmp/whisper_stt"
mkdir -p "$TMP_DIR"

printf '%s' "$HOME/whisper.cpp/models/ggml-small.bin" > "$TMP_DIR/current_model"
printf '%s' "da" > "$TMP_DIR/current_language"
: > "$TMP_DIR/current_extra"

notify-send "STT Mode" "Multilingual auto: small"
