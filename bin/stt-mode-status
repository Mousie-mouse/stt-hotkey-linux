#!/bin/bash

TMP_DIR="/tmp/whisper_stt"
MODEL="$(cat "$TMP_DIR/current_model" 2>/dev/null || echo "$HOME/whisper.cpp/models/ggml-base.en.bin")"
LANG="$(cat "$TMP_DIR/current_language" 2>/dev/null || echo "en")"

notify-send "STT Mode" "Model: $(basename "$MODEL") | Lang: $LANG"
